name: Build and Deploy

on:
  push:
    branches: [ develop ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install and Build
        run: |
          npm install
          npm run build

      - name: Clean up Lightsail
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.LIGHTSAIL_IP }}
          username: ubuntu
          key: ${{ secrets.LIGHTSAIL_SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/localtaskbackend
            rm -rf ./dist ./package.json ./docker-compose.yml ./Dockerfile ./nginx.conf ./migrations

      - name: Copy files to Lightsail
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.LIGHTSAIL_IP }}
          username: ubuntu
          key: ${{ secrets.LIGHTSAIL_SSH_PRIVATE_KEY }}
          source: "./dist,./package.json,./docker-compose.yml,./Dockerfile,./nginx.conf,./migrations"
          target: "/home/ubuntu/localtaskbackend"

      - name: Deploy on Lightsail
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.LIGHTSAIL_IP }}
          username: ubuntu
          key: ${{ secrets.LIGHTSAIL_SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/localtaskbackend
            echo "${{ secrets.PROD_ENV_FILE }}" > .env
            sudo docker compose down --rmi all --remove-orphans
            sudo docker compose up -d --build
            sudo docker exec -it local-task-backend npm run migrate:up
            sudo docker image prune -f
